# Python dependencies for the MET-AI final-dataset notebooks.
#
#   pip install -r requirements.txt
#
# Versions are pinned to the environment that produced the committed notebook
# outputs (Python 3.11.15). Pin exactly to reproduce those outputs bit-for-bit;
# relax to >= if you only need the analyses to run.
#
# R dependencies for analysis_S1.Rmd and hypothesis_tests.R are listed at the
# bottom of this file -- they are not installable with pip.

# ---- core: needed by every notebook ----------------------------------------
pandas==3.0.5
numpy==2.4.6
scipy==1.17.1
statsmodels==0.14.6
matplotlib==3.11.1

# ---- analysis_S1.ipynb / analysis_S1.py only --------------------------------
seaborn==0.13.2

# ---- optional -----------------------------------------------------------------
# pingouin is used by analysis_S1 for a few convenience tests and is guarded by
# a try/except (HAS_PINGOUIN): without it the notebook falls back to scipy and
# still runs to completion.
pingouin==0.6.1

# ---- tooling: only needed to EXECUTE the notebooks headlessly ---------------
# Skip these if you run the notebooks interactively in an existing Jupyter
# install. jupytext keeps analysis_S1.py and analysis_S1.ipynb in sync.
nbconvert==7.17.1
nbformat==5.11.1
ipykernel==7.3.0
jupytext==1.19.5


# =============================================================================
# R dependencies (not pip-installable)
# =============================================================================
#
# analysis_S1.Rmd  -- verified on R 4.3.3 with pandoc 3.1.3
#
#   Required: rmarkdown, knitr, tidyverse, here, effectsize, rstatix, psych,
#             lavaan, patchwork, emmeans, car
#   Optional: ggdist    (half-eye raincloud geom; falls back to geom_violin)
#             DescTools (Jonckheere-Terpstra; falls back to a built-in
#                        implementation validated against a permutation null)
#
#   install.packages(c("rmarkdown","knitr","tidyverse","here","effectsize",
#                      "rstatix","psych","lavaan","patchwork","emmeans","car",
#                      "ggdist","DescTools"))
#
#   On Debian/Ubuntu, apt binaries are much faster than building from source
#   (ggdist and DescTools are not in the Ubuntu archive -- get them from CRAN):
#     apt-get install pandoc r-cran-rmarkdown r-cran-knitr r-cran-tidyverse \
#       r-cran-here r-cran-effectsize r-cran-rstatix r-cran-psych \
#       r-cran-lavaan r-cran-patchwork r-cran-emmeans r-cran-car
#
# hypothesis_tests.R -- base R only, no packages required.
